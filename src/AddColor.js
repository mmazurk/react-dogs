const AddColor = () => {

    // TODO
    // write this so it adds a color to the array
    // I suspect to do this we will need a piece of state
    // But I need to review notes first

    return (
        <div></div>
    )
}

export default AddColor;